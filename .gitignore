# ── Supabase CLI (contiene pooler URL, org id, email de la cuenta) ──
supabase/.temp/

# ── Herramientas de agentes / skills (no son parte del panel) ──
.gstack/
.agents/
.claude/
skills-lock.json

# ── Secretos ──
.env
.env.*
!.env.example

# ── Dependencias / basura de SO ──
node_modules/
.DS_Store
Thumbs.db
